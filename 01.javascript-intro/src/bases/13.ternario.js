
//  TERNARIOS Y NULL CHECK
    // verificar si una variable es null
    let firstName;
    let lastName = 'Casas';

    // Si no tiene valor (undefined) imprime despues de ||
    console.log(`${ firstName || 'No firstName' } ${ lastName || 'No lastName'}`)

    const isActive = true;
    const message = ( isActive ) ? 'Activo' : 'Inactivo'


    console.log(message)








